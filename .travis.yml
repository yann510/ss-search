language: node_js
node_js:
  - 13

before_script:
  - "npx lerna bootstrap --hoist"

script:
  - "npx lerna run build"
  - "npx lerna run prepublishOnly"

after_script:
  - "cd packages/ss-search && cat ./coverage/lcov.info | coveralls"
  - "npx semantic-release"